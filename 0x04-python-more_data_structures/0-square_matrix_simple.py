#!/usr/bin/python3

def square_matrix_simple(matrix=[]):
    new = [[]]
    if matrix = []:
        return None
    for row in matrix:
        for col in row:
          new.append(col ** 2)
            

