#!/usr/bin/python3

def multiple_returns(sentence):
    if sentence = "":
        first_chr = None
        length = len(sentence)
    else:
        length = len(sentence)
        first_chr = sentence[0]
    return length, first_chr
